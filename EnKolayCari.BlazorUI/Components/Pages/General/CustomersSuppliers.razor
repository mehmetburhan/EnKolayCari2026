@page "/customers-suppliers"

<PageTitle>Müşteri & Tedarikçi - EnKolayCari</PageTitle>
<PageHeading Title="Müşteri & Tedarikçi" Subtitle="Cari hesap kartlarınızı görüntüleyin ve yönetin." />

<div class="card p-5">
    <div class="flex items-center justify-between">
        <div>
            <h2 class="text-base font-semibold text-slate-900">Cari Hesaplar</h2>
            <p class="text-xs text-slate-500">Mockup veri &middot; @_accounts.Count kayıt</p>
        </div>
        <LumexButton Color="ThemeColor.Primary" Size="Size.Small">
            <svg class="h-3.5 w-3.5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M12 4.5v15m7.5-7.5h-15" /></svg>
            Yeni Cari
        </LumexButton>
    </div>

    <div class="mt-4">
        <LumexDataGrid Data="@_accounts.AsQueryable()" Hoverable Striped>
            <PropertyColumn Property="@(x => x.Code)" Title="Kod" />
            <PropertyColumn Property="@(x => x.Name)" Title="Ünvan" />
            <PropertyColumn Property="@(x => x.Type)" Title="Tür" />
            <PropertyColumn Property="@(x => x.City)" Title="Şehir" />
            <PropertyColumn Property="@(x => x.BalanceLabel)" Title="Bakiye" Align="Align.End" />
            <PropertyColumn Property="@(x => x.Status)" Title="Durum" />
        </LumexDataGrid>
    </div>
</div>

@code {
    private record CustomerSupplier(string Code, string Name, string Type, string City, string BalanceLabel, string Status);

    private readonly List<CustomerSupplier> _accounts =
    [
        new("120.001", "ABC Ticaret Ltd. Şti.", "Müşteri", "İstanbul", "₺24.560,75", "Aktif"),
        new("120.002", "XYZ Sanayi A.Ş.", "Müşteri", "Bursa", "₺11.500,00", "Aktif"),
        new("320.001", "Mavi Tedarik Ltd.", "Tedarikçi", "Ankara", "-₺3.205,00", "Aktif"),
        new("120.003", "Deniz Yapı Market", "Müşteri", "İzmir", "₺890,00", "Pasif"),
        new("320.002", "Kartal Lojistik A.Ş.", "Tedarikçi", "Kocaeli", "-₺7.420,50", "Aktif"),
        new("120.004", "Yıldız Elektronik", "Müşteri", "Antalya", "₺15.230,00", "Aktif"),
        new("320.003", "Orkide Kırtasiye", "Tedarikçi", "İstanbul", "-₺459,90", "Aktif"),
        new("120.005", "Güneş Tekstil San.", "Müşteri", "Denizli", "₺6.780,40", "Pasif"),
    ];
}
