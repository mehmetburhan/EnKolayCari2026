@page "/products"
@inject ProductStore Store
@inject NavigationManager Navigation

<PageTitle>Ürün - EnKolayCari</PageTitle>
<PageHeading Title="Ürün" Subtitle="Ürün kartlarınızı ve stok bilgilerini görüntüleyin." />

<div class="card p-5">
    <div class="flex items-center justify-between">
        <div>
            <h2 class="text-base font-semibold text-slate-900">Ürünler</h2>
            <p class="text-xs text-slate-500">Mockup veri &middot; @Store.Products.Count kayıt</p>
        </div>
        <button type="button" class="dashboard-button-primary" @onclick='() => Navigation.NavigateTo("/product/new")'>
            <svg class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M12 4.5v15m7.5-7.5h-15" /></svg>
            Yeni Ürün
        </button>
    </div>

    <div class="mt-4">
        <AppDataGrid TItem="ProductStore.Product" Items="@Store.Products" Columns="@_columns" ExportFileName="urunler" SheetName="Ürünler" EmptyText="Ürün bulunamadı." OnEdit="@(item => Navigation.NavigateTo($"/product/edit/{Uri.EscapeDataString(item.Sku)}"))" />
    </div>
</div>

@code {
    private readonly List<GridColumn<ProductStore.Product>> _columns =
    [
        new() { Title = "Stok Kodu", Property = x => x.Sku, Filterable = false },
        new() { Title = "Ürün Adı", Property = x => x.Name, Filterable = false },
        new() { Title = "Kategori", Property = x => x.Category },
        new() { Title = "Birim", Property = x => x.Unit },
        new() { Title = "Stok", Property = x => x.StockQty.ToString(), Align = Align.End, Filterable = false },
        new() { Title = "Fiyat", Property = x => x.PriceLabel, Align = Align.End, Filterable = false },
        new() { Title = "Durum", Property = x => x.Status },
    ];
}
