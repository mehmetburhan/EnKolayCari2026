@page "/document-designs/quote-form"

<PageTitle>Teklif Formu Tasarımı - EnKolayCari</PageTitle>
<PageHeading Title="Teklif Formu Tasarımı" Subtitle="Teklif formu belge şablonlarınızı yönetin." />

<div class="card p-5">
    <div class="flex items-center justify-between">
        <div>
            <h2 class="text-base font-semibold text-slate-900">Teklif Formu Şablonları</h2>
            <p class="text-xs text-slate-500">Mockup veri &middot; @_templates.Count kayıt</p>
        </div>
        <button type="button" class="dashboard-button-primary">
            <svg class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M12 4.5v15m7.5-7.5h-15" /></svg>
            Yeni Şablon
        </button>
    </div>

    <div class="mt-4">
        <AppDataGrid TItem="DocumentTemplate" Items="@_templates" Columns="@_columns" ExportFileName="teklif-formu-tasarimi" SheetName="Teklif Formu Şablonları" EmptyText="Şablon bulunamadı." />
    </div>
</div>

@code {
    private record DocumentTemplate(string Name, string PaperSize, string Language, string LastUpdated, string IsDefaultLabel, string Status);

    private readonly List<DocumentTemplate> _templates =
    [
        new("Standart Teklif Formu", "A4", "Türkçe", "24 Eyl 2026", "Evet", "Aktif"),
        new("Kurumsal Teklif Formu", "A4", "İngilizce", "08 Eyl 2026", "Hayır", "Aktif"),
        new("Hizmet Teklif Formu", "A4", "Türkçe", "15 Ağu 2026", "Hayır", "Pasif"),
    ];

    private readonly List<GridColumn<DocumentTemplate>> _columns =
    [
        new() { Title = "Şablon Adı", Property = x => x.Name, Filterable = false },
        new() { Title = "Kağıt Boyutu", Property = x => x.PaperSize },
        new() { Title = "Dil", Property = x => x.Language },
        new() { Title = "Son Güncelleme", Property = x => x.LastUpdated, Filterable = false },
        new() { Title = "Varsayılan", Property = x => x.IsDefaultLabel },
        new() { Title = "Durum", Property = x => x.Status },
    ];
}
